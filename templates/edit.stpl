<!DOCTYPE html>
<html>
    <% include!("head.stpl"); %>
    <body>
        <header>
            <nav>
                <h1>BookmarkID <%= id %> </h1>
                <form class="search" action="/search">
                    <input type="search" id="search" name="q" placeholder="Search..." />
                    <p>
                        <a class="emoji" href="https://www.sqlite.org/fts5.html#full_text_query_syntax">❔</a>
                    </p>
                </form>
                <ul>
                    <li> <a href="/all">All</a> </li>
                    <li> <a href="/tags">Tags</a> </li>
                    <li> <a href="/about">About</a> </li>
                </ul>
            </nav>
        </header>
        <main>
            <article>
                <form class="edit-bookmark" action="/edit-bookmark/<%= id %>" method="POST">
                    <label for="bookmark-name">Name</label>
                    <input id="bookmark-name" class="edit-bookmark" required type="text" name="name" value="<%= name %>" autocomplete="off">

                    <label for="bookmark-url">URL</label>
                    <input id="bookmark-url" class="edit-bookmark" required type="text" name="url" value="<%= url %>" autocomplete="off">

                    <label for="bookmark-tags">Tags space-separated</label>
                    <input id="bookmark-tags" class="edit-bookmark" type="text"  name="tags"
                        value="<%= tags.iter().cloned().collect::<Vec<_>>().join(" ") %>" placeholder="video programming" autocomplete="on">

                    <button class="btn" type="submit">Save</button>
                </form>
            </article>
        </main>
    </body>
</html>